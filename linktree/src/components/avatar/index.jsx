import React from 'react';

export default function Avatar() {
  // to do add in an image Component
  // possibly with CSS styling around the image to make it clipped to a circle
  return <div>I am an Avatar Image</div>;
}
