import React from 'react';
import ClassicLink from '../classicLink';

export default function MusicStyle() {
  return (
  // ToDo - Similar state based situation as the shows link for open
  // Would again need to generate from a list of providers including their icons, URLs Etc
  // Would need to embed an audio player which was compatable with all relevant APIs
  // Would probably start with react player as per Avid Research Website, quite personaliseable
  // Would need multiple on click handlers for each interactive element
  // Would probably try and break those out into different components

    <ClassicLink>
      Music Style
    </ClassicLink>
  );
}
